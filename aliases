# Nice ls
#   -l Long format with details
#   -A Almost all (include hidden, exclude . and ..)
#   -h Human readable file sizes
#   -G or --color-auto Colourise output on Linux and OSX
#   -F Classify entries with one of the following types
#     * Executable
#     / Directory
#     @ Symlink
#     | Pipe
#     = Socket
alias ls="ls -GlAhF"

# Cat Package.json Scripts
alias cps="cat package.json | jq '.scripts'"

# Open Gatsby's PR page - Hub Gatsby Pulls
alias hgp="hub browse gatsbyjs/gatsby pulls"

# Open Gatsby's issues page - Hub Gatsby Issues
alias hgi="hub browse gatsbyjs/gatsby issues"